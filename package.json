{
	"name": "userglobalsearch",
	"version": "1.0.0",
	"description": "BetterDiscord plugin: the & symbol in Quick Switcher - global user search across mutual servers and DMs, natively integrated UI",
	"author": "florzzzbd",
	"license": "MIT",
	"private": true,
	"scripts": {
		"check": "node --check UserGlobalSearch.plugin.js",
		"test": "node --test tests/*.test.js"
	}
}
